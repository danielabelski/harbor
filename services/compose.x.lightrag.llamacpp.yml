services:
  lightrag:
    depends_on:
      - llamacpp
    environment:
      - LLM_BINDING=openai
      - LLM_BINDING_HOST=http://llamacpp:8080/v1
      - LLM_BINDING_API_KEY=sk-llamacpp
      - LLM_MODEL=${HARBOR_LIGHTRAG_LLAMACPP_MODEL}
