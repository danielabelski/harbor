services:
  librechat-rag:
    depends_on:
      tei:
        condition: service_healthy
    environment:
      # rag_api has a native TEI provider; EMBEDDINGS_MODEL carries the server URL
      - EMBEDDINGS_PROVIDER=huggingfacetei
      - EMBEDDINGS_MODEL=http://tei:80
